/* imports */
import 'package:flutter/material.dart';

/* classes */
class DashboardHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Dashboard Home", style: TextStyle(fontSize: 24)),
    );
  }
}
